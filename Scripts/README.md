# Loon 脚本

## loon_resource_parser.js

资源解析器脚本，供 `Plugins/loon_resource_parser.lpx` 通过远程 URL 引用。

## loon_nodes.js

节点助手脚本，供 `Plugins/loon_nodes.lpx` 通过远程 URL 引用。

## node_sub_example.js

脚本获取节点生成订阅示例脚本，供 `Plugins/node_sub_example.lpx` 通过远程 URL 引用。
